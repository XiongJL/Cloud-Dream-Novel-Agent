# 章节写作修复与真实复测（2026-09-07）

## 已完成的代码修复

- Responses 必须收到 `response.completed` 才认定成功。delta、output_text.done 或 `[DONE]` 后直接 EOF 均报告 `stream_incomplete`，保留部分正文及响应证据；不把未完成正文发布为成功草稿。明确输出额度截断仍归类 `MODEL_OUTPUT_TRUNCATED`。
- 手动输出额度按配置使用，仍受模型能力与上下文余量限制。章节/素材首次 8192、增额一次的策略仅用于自动模式。
- 用户明确要求为已定位的空白首章写完整正文时，允许从空正文起稿，使用现有章节 ID。目标信息缺少 `hasContent` 时核对已读取的章节 ID；普通续写仍需正文或可用前文。
- Agent 对话和独立规划接入统一 `plan` 预算，去掉 2600/2400 的隐藏限制。自动模式为 4096→8192；当前手动 16384 按 16384 请求。上下文装配预留恢复额度，窗口不足时同步下调两次额度，避免恢复请求超出预留空间。

未更改用户 AI 配置；当前使用 `deepseek-v4-flash`、手动上限 16384。

## 真实调用证据

测试作品：`Agent完整章节验收-雾港来信-20260905`；章节：`第一章 七分钟后的来信`。

- 会话 `conv_1788671161625_0u2ahc` 的新计划正确路由为一个 `chapter.continuation`，带 `editorialReview=true`，没有多余素材草稿生产者。
- 运行 `run_6c55a11fdf1347e7` 在修复首章守卫后生成草稿 `91f6e8e5-3633-4f6c-a703-c574cbfe7fa0`：4021 字符、3415 个汉字。生成返回 `finishReason=completed`，请求额度 16384，实际输出 16220 token，耗时约 188 秒。
- 后续自动审校报“生成达到本次输出额度”，运行状态 `failed`、`completionKind=partial`。已生成的草稿仍可打开审核，正式章节仍为空；没有因整体失败丢失草稿或直接写回正文。
- 9 月 7 日重启应用，草稿和失败运行均恢复。通过应用的 `agent.generate_consistency_review` 对同一保存草稿单独重试，成功返回结构化审校报告；没有重新生成正文或更改额度。
- 独立重试不等同于原 Runtime 运行恢复成功，原失败状态保持真实。该运行没有可用的节点恢复入口（`recovery=null`）。

## 内容验收发现

初稿超过要求的 1800—2200 汉字；录音日期表述混乱、无电自动播放缺少电源线索、底噪推理有跳跃，门外声音后还有较长尾声。已提交针对性修订意见。模型审校中的照明意见与原文“来电后检修”不符，未直接采用。

第一次针对性修订生成 1686 字符、1371 个汉字，核心情节改善但篇幅偏短。随后通过 `agent.revise_draft` 发起 Runtime 修订运行 `run_cfba20259be34e22`，给出明确增量及锂电池/定时器供电修正意见；该运行 **completed**，生成最终草稿 `e1f17dd6-31d0-4b69-b6a0-f374f17a6395`，2224 字符、**1826 个汉字**。五个要求的关键情节、有限视角和章末自己的声音均保留。

字数按 JavaScript `/\p{Script=Han}/gu` 实测；草稿审核显示的 2224“字”包含标点、数字及换行，写作编辑器加载完成后显示 2165“字数”，与纯汉字计数口径均不同。

最终通过界面“打开审核→确认写回”写入测试章节：章节版本 1→2，正式内容与批准草稿 `payload.content` 完全一致，草稿状态 `committed`，仅有一条写回记录。刷新后内容不变；关闭并重新启动 Electron 后，内容、版本、草稿已写回状态及修订运行 completed 状态均保持。复测期间未观察到 renderer pageerror。正文副本见 `output/playwright/chapter-writing-20260906/第一章-七分钟后的来信.md`。

本轮不以模型评分代替人工核对。证据及修订结果保存在 `output/playwright/chapter-writing-20260906/`。

## 回归验证

- `task-output-budget-test.mjs` 通过：手动章节/素材/规划/审校额度，自动恢复与上下文限额，以及执行真实规划方法的调用额度断言。
- `http-responses-stream-test.mjs`、`http-provider-retry-contract-test.mjs` 通过。
- `agent-context-assembler-test.mjs` 通过。
- `pnpm run typecheck` 通过。
- `pnpm exec vite build` 通过（含 main/preload；仅已有 bundle 体积提示）。
- `test_draft_toolchains.py`：14 passed；1 项依赖弃用警告。

## 验收结论与限制

- 已通过真实模型生成、保存草稿审校、Runtime 按意见修订、界面审核写回、刷新与重启持久化的分段流程。
- 首次自动运行曾在审校阶段失败，本次借助独立审校调用及审核意见继续完成，不能标记“无人工介入的一次完整流程通过”；新预算代码下尚未从空白输入重跑完整单次流程。
- 自动审校与篇幅验收仍有不足：模型可能遗漏明确字数限制或给出不准确意见，当前没有自动汉字计数达标关卡。写作提示还会在结构化目标缺失时给出默认 500 字，与自然语言中的完整章节长度要求可能竞争；本次已用明确修订意见达标，默认目标与验收策略宜后续统一。
- 原审校失败被包装成 `NODE_FAILED`，`recovery=null`，没有直接恢复失败审校节点的 UI 入口。已保存草稿可继续审阅和修订，但尚未验证原运行的节点级恢复。
